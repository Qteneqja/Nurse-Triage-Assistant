# FastAPI Backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0          # Production multi-worker server
pydantic>=2.10.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6  # Required for Twilio form data
twilio>=8.0.0             # Twilio SDK (signature validation + utilities)

# LLM Integration
openai>=1.10.0
httpx>=0.26.0
jinja2>=3.1.3
tenacity>=8.2.0,<9.0.0  # tenacity 9+ requires tornado at import time; pin to v8
tornado>=6.0             # required by tenacity[tornado] on Windows

# Environment Management
python-dotenv>=1.1.1,<2.0.0

# Phase 3: Persistent Storage
sqlalchemy>=2.0.49
alembic>=1.13.0
psycopg2-binary>=2.9.0   # PostgreSQL driver (binary for easy install)

# Azure Blob Storage (persistent report storage in production)
azure-storage-blob>=12.19.0

# Optional: SQLite support (if needed)
aiosqlite==0.19.0

# Development
pytest>=9.0.0,<10.0.0
pytest-asyncio>=1.3.0,<2.0.0
deepeval==4.0.2
requests>=2.31.0         # used by test_intake_flow.py (live-server integration test)

# Error Monitoring (Step 4 - Production Hardening)
sentry-sdk>=1.40.0        # Error monitoring with PHI scrubbing
